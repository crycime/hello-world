// Generated by CoffeeScript 2.3.1
(function() {
  var createLogger, defaultLogger, format, transports;

  ({transports, format, createLogger} = require("winston"));

  defaultLogger = function(label) {
    var bestFormat, uniFormat;
    uniFormat = format.printf(function(info) {
      return `${info.timestamp} [${info.label}] ${info.level}: ${info.message}`;
    });
    bestFormat = format.combine(format.colorize(), format.label({label}), format.timestamp(), uniFormat);
    return createLogger({
      transports: [
        new transports.Console({
          format: bestFormat,
          colorize: true,
          label
        })
      ]
    });
  };

  module.exports = {defaultLogger};

}).call(this);
